/**
 * @typedef {Object} Point
 * @property {number} x
 * @property {number} y
 */

/**
 * @description
 * Maze Solver is a problem that involves finding a path from the start point to the end point in a maze.
 * its a great use case for recursion.
 *
 * @param {string[]} maze
 * @param {string} wall
 * @param {Point} start
 * @param {Point} end
 * @returns {Point[]}
 */
export const maze_solver = (maze, wall, start, end) => {
  console.log(maze, wall, start, end)
  return []
}

