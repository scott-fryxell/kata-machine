/**
 * @param {import('./types').WeightedAdjacencyList} graph
 * @param {number} source
 * @param {number} needle
 * @returns {number[] | null}
 */
export default function dfs(graph, source, needle) {

}
