/**
 * @template {string | number} T
 * @template V
 */
export default class Map {
  constructor() {
  }

  /**
   * @param {T} key
   * @returns {V | undefined}
   */
  get(key) {
  }

  /**
   * @param {T} key
   * @param {V} value
   * @returns {void}
   */
  set(key, value) {
  }

  /**
   * @param {T} key
   * @returns {V | undefined}
   */
  delete(key) {
  }

  /**
   * @returns {number}
   */
  size() {
  }
}
